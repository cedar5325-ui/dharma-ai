"use client";
import { useEffect, useState } from "react"; import { AdminHeader } from "@/components/AdminHeader";
type Purchase={id:string;materialTitle:string;amount:number;status:string;createdAt:string;paidAt?:string;downloadCount:number;};
export default function AdminPurchasesPage(){const[items,setItems]=useState<Purchase[]>([]);const[message,setMessage]=useState("");useEffect(()=>{fetch("/api/admin/purchases",{cache:"no-store"}).then(r=>r.json()).then(j=>{if(!j.ok){setMessage(j.message||"구매 내역을 불러오지 못했습니다.");return;}setItems(j.items||[]);})},[]);return <><AdminHeader/><main className="section white"><div className="sectionLabel">ADMIN · PURCHASES</div><h1 className="pageTitle">구매 / 다운로드 내역</h1><p className="subText">자료 1건 20,000원 결제 요청, 결제 완료, 다운로드 기록을 확인합니다.</p>{message&&<p className="loginNotice">{message}</p>}<div className="formCard" style={{marginTop:28,overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse"}}><thead><tr><th align="left">자료</th><th align="left">금액</th><th align="left">상태</th><th align="left">생성일</th><th align="left">결제일</th><th align="left">다운로드</th></tr></thead><tbody>{items.map(item=><tr key={item.id} style={{borderTop:"1px solid #e1ecff"}}><td style={{padding:"12px 8px"}}>{item.materialTitle}</td><td>{item.amount.toLocaleString()}원</td><td>{item.status}</td><td>{new Date(item.createdAt).toLocaleString()}</td><td>{item.paidAt?new Date(item.paidAt).toLocaleString():"-"}</td><td>{item.downloadCount}회</td></tr>)}</tbody></table>{items.length===0&&<p>구매 내역이 없습니다.</p>}</div></main></>}
